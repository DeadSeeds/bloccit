<h1>Ask a question...</h1>

<div class="row">
  <div class="col-md-4">
    <p>Guidelines for questions:</p>
    <ul>
      <li>Have you searched Google?</li>
      <li>Have you searched the FAQ?</li>
      <li>Are you sure this hasn't already been answered?</li>
      <li>If none of the above, proceed...</li>
  </div>
  <div class="col-md-8">
    <%= form_for @question do |f| %>
      <div class="form-group">
        <%= f.label :title %>
        <%= f.text_field :title, class: 'form-control', placeholder: "Title" %>
      </div>
      <div class="form-group">
        <%= f.label :body %>
        <%= f.text_area :body, rows: 8, class: 'form-control', placeholder: "Words" %>
      </div>
      <div class="form-group">
        <%= f.label :resolved %>
        <%= f.check_box :resolved %>
      </div>
      <div class="form-group">
        <%= f.submit "Save", class: 'btn btn-success' %>
      </div>
    <% end %>
  </div>
</div>
